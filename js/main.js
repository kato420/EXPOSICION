document.addEventListener("DOMContentLoaded", () => {
  const sectionNosotros = document.querySelector(".section-nosotros");
  if (sectionNosotros) {
    // Añadir la clase 'visible' cuando el documento se haya cargado completamente
    sectionNosotros.classList.add("visible");
  }
});
